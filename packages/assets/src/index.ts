// Export the font CSS for import in Next.js apps
export { default as fontCSS } from './fonts.css';

// You can also export individual font files if needed
export const fonts = {
  uberMove: {
    light: './src/fonts/UberMove-Light.woff2',
    regular: './src/fonts/UberMove-Regular.woff2',
    medium: './src/fonts/UberMove-Medium.woff2',
    bold: './src/fonts/UberMove-Bold.woff2',
  }
};